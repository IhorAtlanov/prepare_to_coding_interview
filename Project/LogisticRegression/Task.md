1. Теоретична база та математикаПеред тим як кодити, випиши собі три основні кити, на яких тримається алгоритм:Лінійна комбінація: $z = Xw + b$, де $X$ — матриця ознак, $w$ — ваги, $b$ — зсув (bias).Функція активації (Сигмоїда): Переводить будь-яке число у проміжок $[0, 1]$, тобто у ймовірність.$$\sigma(z) = \frac{1}{1 + e^{-z}}$$Функція втрат (Log-Loss / Binary Cross-Entropy): Вимірює, наскільки помиляється модель.$$J(w, b) = -\frac{1}{m} \sum_{i=1}^{m} \left[ y^{(i)} \log(\hat{y}^{(i)}) + (1 - y^{(i)}) \log(1 - \hat{y}^{(i)}) \right]$$2. Покроковий план реалізації (Псевдокод)Крок 1: Підготовка данихНе бери одразу великі датасети. Згенеруй простий синтетичний набір для бінарної класифікації.Зроби матрицю ознак $X$ розмірністю $(m, n)$, де $m$ — кількість прикладів, $n$ — кількість ознак.Зроби вектор правильних відповідей $y$ розмірністю $(m, 1)$, який містить тільки $0$ та $1$.Важливо: Нормалізуй $X$ (масштабуй ознаки), інакше градієнтний спуск буде працювати дуже повільно або розходитися.Крок 2: Ініціалізація параметрівТобі потрібні змінні, які модель буде вчити.Створи вектор ваг $w$ розмірністю $(n, 1)$. Можна заповнити нулями або маленькими випадковими числами.Створи змінну для зсуву $b$ (скаляр), ініціалізуй нулем.Крок 3: Прямий прохід (Forward Propagation)Для кожного об'єкта з $X$ потрібно порахувати передбачення:Порахуй лінійну комбінацію: $z = X \cdot w + b$ (використовуй матричне множення).Пропусти $z$ через сигмоїду: $\hat{y} = \sigma(z)$. Тепер $\hat{y}$ — це вектор передбачених ймовірностей.Крок 4: Обчислення помилки (Compute Cost)Реалізуй функцію Log-Loss за формулою вище.Порада для коду: Додавай маленьке значення (наприклад, $1e^{-15}$) всередину log(), щоб уникнути помилки log(0), якщо модель видасть чистий 0 або 1.Крок 5: Зворотний прохід (Backward Propagation / Gradients)Тобі потрібно знайти похідні функції втрат по відношенню до $w$ та $b$, щоб знати, куди їх змінювати. Матричні формули градієнтів виглядають дуже просто:$dw = \frac{1}{m} X^T (\hat{y} - y)$$db = \frac{1}{m} \sum(\hat{y} - y)$Крок 6: Оновлення параметрів (Gradient Descent)Крок у бік, протилежний градієнту:$w = w - \alpha \cdot dw$$b = b - \alpha \cdot db$Де $\alpha$ (learning rate) — це швидкість навчання (наприклад, $0.01$ або $0.1$).Крок 7: Цикл навчання (Training Loop)Об'єднай кроки 3–6 у цикл for на $N$ ітерацій (епох):Python# Псевдокод
for epoch in range(epochs):
    # 1. Forward (y_pred = sigmoid(Xw + b))
    # 2. Cost (порахуй лосс для моніторингу)
    # 3. Backward (порахуй dw, db)
    # 4. Update (w = w - lr * dw...)
Крок 8: Передбачення (Predict)Коли модель навчена (ваги $w$ та $b$ оптимізовані), функція predict(X) має:Порахувати ймовірності $\hat{y} = \sigma(Xw + b)$.Конвертувати їх у класи: якщо $\hat{y} \ge 0.5$, то клас $1$, інакше — клас $0$.





Покроковий розбір коду (Лінія за лінією)1. Ініціалізація та структураPythonimport numpy as np
Що робить: Імпортує бібліотеку NumPy під коротким ім'ям np. Вона потрібна для швидких матричних обчислень (векторизації), щоб не писати повільні цикли for для кожного математичного рівняння.Pythonclass LogisticRegressionFromScratch:
Що робить: Оголошує наш клас моделі. Оформлення у вигляді класу дозволяє зберігати навчені ваги всередині об'єкта, як це зроблено у бібліотеці Scikit-Learn.Python    def __init__(self, learning_rate=0.01, epochs=1000):
        self.lr = learning_rate
        self.epochs = epochs
        self.weights = None
        self.bias = None
        self.loss_history = []
self.lr та self.epochs: Гіперпараметри моделі. Швидкість навчання ($\alpha$) визначає розмір кроку градієнтного спуску, а епохи — скільки разів модель пройдеться по всьому датасету.self.weights та self.bias: Параметри моделі ($w$ та $b$). Спочатку вони рівні None, бо ми дізнаємося кількість ознак в даних лише під час запуску навчання.self.loss_history: Порожній список, куди ми будемо записувати помилку на кожній ітерації, щоб потім побудувати графік її падіння.2. Функція активаціїPython    def _sigmoid(self, z):
        return 1 / (1 + np.exp(-z))
Що робить: Реалізує математичну формулу сигмоїди: $\sigma(z) = \frac{1}{1 + e^{-z}}$.Деталі: Нижнє підкреслення на початку назви (_sigmoid) натякає, що це внутрішній (приватний) метод класу. np.exp(-z) експонує кожен елемент масиву z. Якщо на вхід прийде велике від'ємне число, результат буде близьким до 0, якщо велике додатне — до 1.3. Метод навчання (fit)Python    def fit(self, X, y):
        num_samples, num_features = X.shape
Що робить: Приймає матрицю ознак $X$ та вектор відповідей $y$. За допомогою атрибуту .shape дізнається кількість об'єктів (num_samples, рядочки) та кількість ознак (num_features, стовпчики).Python        self.weights = np.zeros((num_features, 1))
        self.bias = 0.0
Що робить: Ініціалізує ваги. Створює вектор-стовпчик self.weights з нулів розмірністю $(n, 1)$, де $n$ — кількість ознак. Зсув self.bias встановлює в 0.0 (це звичайне число з рухомою комою).Python        y = y.reshape(-1, 1)
Що робить: Гарантує, що вектор $y$ має розмірність $(m, 1)$, тобто є вектором-стовпчиком. Це критично для правильного матричного віднімання та множення далі в коді.Python        for epoch in range(self.epochs):
Що робить: Запускає головний цикл градієнтного спуску, який повториться задану кількість разів.Крок 3.1: Прямий прохідPython            z = np.dot(X, self.weights) + self.bias
Що робить: Обчислює лінійну комбінацію. np.dot(X, self.weights) — це матричне множення матриці $X$ розміру $(m, n)$ на стовпчик ваг $(n, 1)$. На вихід отримуємо вектор розміру $(m, 1)$, до кожного елемента якого додається скаляр self.bias.Python            y_pred = self._sigmoid(z)
Що робить: Пропускає лінійні передбачення через сигмоїду. Отримує вектор y_pred розміру $(m, 1)$, де кожен елемент — це ймовірність належності до класу 1 (значення від 0 до 1).Крок 3.2: Розрахунок Log-LossPython            epsilon = 1e-15
            y_pred = np.clip(y_pred, epsilon, 1 - epsilon)
Що робить: Захищає код від критичної помилки обчислень. Якщо модель впевнена на 100%, вона видасть y_pred = 1.0 або 0.0. Але математично $\log(0)$ дорівнює мінус нескінченності. np.clip обрізає значення: все, що менше $10^{-15}$, стає $10^{-15}$, а все, що більше $0.999999999999999$, стає таким.Python            loss = -1 / num_samples * np.sum(y * np.log(y_pred) + (1 - y) * np.log(1 - y_pred))
            self.loss_history.append(loss)
Що робить: Рахує середню бінарну крос-ентропію (Log-Loss) для поточної епохи та додає її в історію. Це чиста реалізація формули $J(w,b)$.Крок 3.3: Зворотний прохід (Градієнти)Python            dw = (1 / num_samples) * np.dot(X.T, (y_pred - y))
Що робить: Знаходить градієнт функції втрат по відношенню до ваг ($dw$). Спочатку рахується чиста помилка (y_pred - y). Потім матриця $X$ транспонується (X.T), щоб її розмірність стала $(n, m)$, і множиться на вектор помилок $(m, 1)$. Результат ділиться на кількість прикладів num_samples для усереднення. Отримуємо вектор градієнтів розміру $(n, 1)$.Python            db = (1 / num_samples) * np.sum(y_pred - y)
Що робить: Знаходить градієнт для зсуву ($db$). Оскільки зсув не залежить від ознак $X$, ми просто додаємо (np.sum) всі помилки передбачень разом і ділимо на їх кількість.Крок 3.4: Оновлення параметрівPython            self.weights -= self.lr * dw
            self.bias -= self.lr * db
Що робить: Крок градієнтного спуску. Знак -= означає, що ми рухаємося проти градієнта (тобто у бік зменшення помилки). Множення на self.lr масштабує цей крок.4. ПередбаченняPython    def predict_proba(self, X):
        z = np.dot(X, self.weights) + self.bias
        return self._sigmoid(z)
Що робить: Публічний метод. Приймає нові дані $X$ і повертає сирі ймовірності (значення від 0 до 1). Робить точно те саме, що й перші два рядки всередині циклу навчання.Python    def predict(self, X):
        y_proba = self.predict_proba(X)
        return np.where(y_proba >= 0.5, 1, 0)
Що робить: Повертає фінальні мітки класів (0 або 1).Деталі: Спочатку викликає predict_proba, щоб отримати ймовірності. np.where(умова, якщо_так, якщо_ні) працює як тернарний оператор для масивів: якщо ймовірність більша або дорівнює 0.5, записує в масив 1, в іншому випадку — 0.